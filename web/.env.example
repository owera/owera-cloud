# Owera Agentic — web/ environment

# Upstream API base URL. Defaults to the local Go API in dev.
NEXT_PUBLIC_API_URL=http://localhost:8080

# Auth provider stub. Wire to a real provider (clerk | workos) in a follow-on PR.
# Allowed values: mock | clerk | workos
NEXT_PUBLIC_AUTH_PROVIDER=mock
